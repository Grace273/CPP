#include <bits/stdc++.h>
using namespace std;
int main() {
    float float_var =1.1;

    cout << "Ceil:" << ceil(float_var) << endl;
    cout << "Round: " << round(float_var) << endl;
    cout << "FLoor: " << floor(float_var) << endl;

    //int(integer) by default uses floor
}