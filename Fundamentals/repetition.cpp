#include <bits/stdc++.h>
using namespace std;

int main() {
    // i++ is the same as i = i + 1;
    // i+=2 is the same as i = i + 2;
    
     //for (int i = 9; i < 20; i+=4) {
        //cout << i << endl;
    //}

    for (int i = 10; i >= 0; i--) {
        cout << i << endl;
    }
}